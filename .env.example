# Optional: full backend origin when not using Vite proxy (production).
# Dev: leave unset so requests go to same origin and Vite proxies `/api` → http://127.0.0.1:5001
# VITE_API_URL=http://127.0.0.1:5001
